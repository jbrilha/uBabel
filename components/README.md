ESP-IDF automagically scans this dir for components, it would be possible to make
"lib" the components dir but the IDF will try to make a component out of the 
pico-specific stuff so.... symlinks it is
